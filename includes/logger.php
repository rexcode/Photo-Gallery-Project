<?php  
// class Logger{

	// public $log_user;
	// public $log_time = strftime("%Y - %m - %d %H : %M : %S", time() );
	// public $message =  $this->log_time . " | LOGIN : {$log_user} Logged in.\n";
	
	// public function logging_action($this->log_user, $this->message=""){
	// 	$file = "../logs/log.txt";
	// 	if (file_exists($file) && is_writable($file)) { 
	// 		if ($handle = fopen($file, 'a')) {
	// 				fwrite($handle, $this->message);
	// 		} else {
	// 			echo "File could not be accessed.";
	// 		} fclose($handle);
	// 	} else {
	// 	if ($handle = fopen($file, 'a'){
	// 			fwrite($handle, $this->message);
	// 		} else {
	// 			echo "File could not be accessed.";
	// 	}
	// }

?>